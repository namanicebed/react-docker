language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t namanicebed/react-docker -f Dockerfile.dev .

script:
  - docker run -e CI=true namanicebed/react-docker npm run test